# Quotas and Abuse Prevention

Configurable quotas include devices per tenant, enrollment tokens, API request
rate, telemetry bytes per day, telemetry cardinality, artifact storage, artifact
size, concurrent jobs, concurrent deployments, notification rate, and diagnostic
bundle size.

Quota errors are explicit and auditable. Telemetry label cardinality is capped
server-side before samples are accepted.
